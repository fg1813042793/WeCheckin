// Package model keeps compatibility aliases for historical callers.
//
// Concrete model definitions live in domain subpackages below this directory.
// New code can import a domain subpackage directly when it does not need the
// legacy model.User/model.Role style facade.
package model
