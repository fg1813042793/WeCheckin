# 后端 API 地址
# 留空时请求保持为 /api/v2/... 同源路径，本地开发会走 Vite /api/v2 代理
# 生产环境如需独立 API 域名，可填写完整地址，例如 https://api.example.com
VITE_API_BASE_URL=

# Vite 本地代理目标，默认后端端口为 8083
VITE_DEV_PROXY_TARGET=http://localhost:8083

# 钉钉企业 CorpId，用于 H5 微应用免登授权码
VITE_DINGTALK_CORP_ID=
